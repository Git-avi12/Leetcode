class Solution(object):
    def plusOne(self, digits):
        sum=0
        l=[]
        cnt=len(digits)-1
        for i in digits:
            sum+=i*10**cnt
            cnt-=1
        sum+=1
        sumstr=str(sum)
        for i in sumstr:
            l.append(int(i))
        return l
