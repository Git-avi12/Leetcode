class Solution(object):
    def productExceptSelf(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        n=len(nums)
        out=[1]*n
        p=1
        for i  in range(1,n):
            out[i]=out[i-1]*nums[i-1]
        for i in range(n-2,-1,-1):
            p*= nums[i+1]
            out[i]*=p
        return out

        
        



        
