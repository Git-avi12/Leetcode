from collections import Counter
class Solution(object):
    def frequencySort(self, s):
        """
        :type s: str
        :rtype: str
        """
        d = Counter(s)
        l = sorted(d.items(),key=lambda row: (-row[1],row[0]))
        st = "".join(char*freq for char,freq in l)
        return st        
